
{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Blazor WASM: Launch (Chrome)",
      "type": "blazorwasm",
      "request": "launch",
      "cwd": "${workspaceFolder}",
      "browser": "edge",
      "url": "http://localhost:5238",
      "timeout": 60000
    }
  ]
}